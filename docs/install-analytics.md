# Install analytics

**This document has moved.** The `dsh1024` CLI, the in-harness 1024Store plugin, and the
Worker that ingests install telemetry now live in
[imsai-sh/dsh-1024store](https://github.com/imsai-sh/dsh-1024store), and this document moved
with them:

**→ [docs/install-analytics.md in dsh-1024store](https://github.com/imsai-sh/dsh-1024store/blob/main/docs/install-analytics.md)**

This stub stays at this URL permanently: already-published versions of the `dsh1024` npm
package print this address in their first-run telemetry notice, so it must keep resolving.
Do not delete or rename this file.
