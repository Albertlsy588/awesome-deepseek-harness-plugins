# Optional for fallback-only local development; required for production discovery.
GITHUB_TOKEN=

# Required for install-event ingestion. Generate a stable value with:
# openssl rand -hex 32
INSTALL_CLIENT_HASH_SECRET=
